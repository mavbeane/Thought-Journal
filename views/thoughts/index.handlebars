<h1>Thought Journal</h1>

{{#if username}}
    Hello {{username}}
{{/if}}
{{#unless logged_in}}
    <p>Sign up</p>
{{/unless}}

{{#unless logged_in}}
    <h1>WELCOME TO THOUGH JOURNAL</h1>
    </p>Thought journal description</p>
{{/unless}}

<!--{{#if logged_in}}-->
    {{#each thoughts}}
            {{this.entry}}
            {{this.word}}
            {{this.colorHex}}    
    {{/each}}
<!--{{/if}}-->

<div class="container">
        <div class="row">
            <div class="col-xs-12 col-md-12">
                <h1>Thought journal</h1>
                <button id="add" class="btn btn-primary btn-lg btn-block" data-toggle="modal" data-target="#myModal">Add Entry</button>
                <div class="container">

                    <div class="row">
                        <div class="col-xs-12 col-md-12">
                            <h2>FEED</h2>
                            <div id="feed">

                            </div>
                        </div>
                    </div>

                </div>

                <!-- Modal -->
              